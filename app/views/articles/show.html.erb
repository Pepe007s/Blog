<div class="jumbotron">
	<h1><%= @article.title%></h1>
	<p><%= @article.body%></p>

	<%=	link_to "Editar", edit_article_path(@article), class:"btn btn-info"%>
	<%=	link_to "Atras", root_path, class:"btn btn-danger"%>
</div>
